{
  "name": "agent-escrow-protocol",
  "version": "1.0.0",
  "description": "Production-ready escrow protocol for agent payments with USDC support",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:base": "hardhat run scripts/deploy.js --network base",
    "deploy:base-sepolia": "hardhat run scripts/deploy.js --network baseSepolia",
    "verify:base": "hardhat run scripts/verify.js --network base",
    "verify:base-sepolia": "hardhat run scripts/verify.js --network baseSepolia",
    "clean": "hardhat clean"
  },
  "keywords": [
    "solidity",
    "hardhat",
    "escrow",
    "usdc",
    "base",
    "ethereum"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.1"
  }
}
